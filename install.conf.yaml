- clean: ['~']

- link:
    ~/.profile: profile
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.tmux.conf: tmux.conf
    ~/.config/nvim:
        path: config/nvim
        create: true
    ~/.config/kitty:
        path: config/kitty
        create: true


